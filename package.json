{
  "name": "vault-task-board",
  "version": "1.1.0",
  "description": "Kanban board with projects, task dependencies, subtasks, and a public API for companion plugins.",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/FernandoRP-nan/vault-task-board.git"
  },
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.17",
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  }
}
